import React from 'react';
import Banner from '../Components/Banner';

const home = () => {
  return (
    <section className='section'>
      <Banner></Banner>
    </section>
  );
};

export default home;
